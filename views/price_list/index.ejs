
<% layout('../layout/base_layout') -%>

<% block("pageSelected", "price_list") %>
<% block("title", "Look - CENNIK USŁUG") %>

<%- partial("partials/_price_left_table", { epil_items: epil_items }) %>

<% services.forEach(function(service, key) { if(((key+1)%2) == 0) { %>
	
		<%- partial("partials/_price_left_block", {service: service}) %>

	<% } else { %>
		
		<%- partial("partials/_price_right_block", {service: service}) %>
	
<% } }) %>
